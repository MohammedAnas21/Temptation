# Optional: host the APK on a dedicated file store (S3, R2, GitHub Releases, etc.)
# and set the public URL here. If left empty, the build expects temptations-cafe.apk
# to exist in this folder and will serve it directly from Vercel.
APK_DOWNLOAD_URL=
